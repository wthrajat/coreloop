import { LearningSettings } from "@/components/learning-settings";

export const metadata = { title: "Set up your profile" };
export default function OnboardingPage() {
  return <LearningSettings onboarding />;
}
